import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    mensagem: {
        fontSize: 25,
        margin: 10,
        color: 'orange',
    },
    viewmensagem: {
        borderwidth: 2,
        borderColor: 'orange',
        borderRadius: 20,
    },



});

export default styles;