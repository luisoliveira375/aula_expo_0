import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
    container: {
        backgroundColor: '#0f0',
        padding: 10,
    },
    texto: {
        color: '#fff',
        fontsize: 20,
    },

});

export default style;